import {colors, createTheme} from "@mui/material";

export const colorTheme = createTheme({
    palette: {
        primary: {
            main: colors.blue[800],
        },
        mode: "dark",
    },
});
